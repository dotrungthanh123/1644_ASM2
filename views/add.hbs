<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
  <title>Add</title>
</head>

<body>
  {{!-- NAVBAR --}}
  <nav class="navbar navbar-expand bg-body-secondary">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">ATN</a>
      <ul class="navbar-nav my-auto me-auto">
        <li class="nav-item">
          <a class="nav-link" href="/add">Add</a>
        </li>
      </ul>
  </nav>

  <div class="row m-0">
    <div class="col-8 col-sm-6 col-md-4 container mt-4 border border-5 rounded-4 border-dark-subtle pb-3 pt-2">
      <h1 class="text-center fw-light mb-3">ADD NEW ITEM</h1>
      {{!-- ADD FORM --}}
      <form action="/add" method="post" onsubmit="return isValid()">
        <div class="mb-3">
          <input type="hidden" name="id" value="{{item._id}}">
          <label class="form-label">Name</label>
          <input class="form-control" name="item" value="{{item.item}}">
        </div>
        <div class="mb-3">
          <label class="form-label">Price</label>
          <input type="number" step="any" value="{{item.price}}" class="form-control" name="price">
        </div>
        <div class="mb-3">
          <label class="form-label">Description</label>
          <textarea class="form-control" name="description" rows="3">{{item.description}}</textarea>
        </div>
        <div class="mb-3">
          <label class="form-label">Image URL</label>
          <input class="form-control" value="{{item.img}}" name="img">
        </div>
        <div class="text-center">
          <button type="submit" class="btn btn-secondary">Add</button>
        </div>
      </form>
    </div>
  </div>
  <script>
    function isValid() {
      if (String(document.getElementsByName('item').value).length < 10) {
        alert("message1")
        return false
      }
      return true
    }
  </script>
  {{#if alert}}
    <script>
      alert('message2')
    </script>
  {{/if}}
</body>

</html>